# Requires V.vertices, raws from square with vertices $vtx1-4 #
@draw_square [$vtx1, $vtx2, $vtx3, $vtx4] {
    @cursor [$vtx1]

    @with [MB_LEFT] {
        @cursor [$vtx2]
        @cursor [$vtx3]
        @cursor [$vtx4]
        @cursor [$vtx1]
    }
}